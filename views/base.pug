doctype html 
html
    head
        //- title Natours | #{tour}
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href='/css/style.css')
        link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
        link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')   
        title  Natours | #{title}

    body 
        //- h1= tour this comment does not get output to html; //-
        // This is visible as comment in html output
        //- h1= user.toUpperCase()
        //- //- h1 The Aurora Viewer

        //- -const x = 9;
        //- h2= 2*x
        //- p This is just some text
        // Header
        include _header

        // Content
        block content 
         h1 This is a placeholder text from base.pug

        // Footer
        include _footer
        
                

